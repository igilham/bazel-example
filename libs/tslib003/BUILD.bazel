package(default_visibility = ["//visibility:public"])

load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = 'tslib003',
    srcs = [
        'index.ts',
    ],
    deps = [
        '//libs/tslib001'
    ],
)
