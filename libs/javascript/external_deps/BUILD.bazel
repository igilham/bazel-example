load("@build_bazel_rules_nodejs//:index.bzl", "pkg_npm")

filegroup(
    name = 'src',
    srcs = [
        'index.js',
    ],
)

pkg_npm(
    name = 'pkg',
    srcs = [
        'package.json'
    ],
    deps = [
        ':src',
        '@npm//lodash',
    ],
    visibility = ['//visibility:public'],
)
