load("@build_bazel_rules_nodejs//:index.bzl", "npm_package")
load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = 'tslib002',
    module_name = 'typescript-external-deps',
    srcs = [
        'index.ts',
    ],
    deps = [
        '@npm//lodash'
    ],
    visibility = ['//visibility:public'],
)
