load("@build_bazel_rules_nodejs//:index.bzl", "npm_package")
load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = 'tslib001',
    module_name = 'typescript_no_deps',
    srcs = [
        'index.ts',
    ],
    deps = [],
    visibility = ['//visibility:public'],
)
